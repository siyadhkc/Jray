{
  "name": "jray",
  "version": "0.1.0",
  "description": "A modern JSON flattener and query tool — like gron meets jq",
  "author": "Your Name",
  "license": "MIT",

  "bin": {
    "jray": "./src/cli.ts"
  },

  "scripts": {
    "dev": "bun run src/cli.ts",
    "test": "bun test",
    "build": "bun build src/cli.ts --compile --outfile dist/jray"
  },

  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/bun": "latest"
  }
}